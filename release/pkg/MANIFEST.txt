recamera-ext-api  v1.6.0
Built: 2026-08-17   Target: reCamera Pro (RV1126B / recamera_v2)

ARTIFACTS (md5)
  rkipc                             f683352a9d062a05a3df1f8df22d7d53   15585904 B  -> /oem/usr/bin/rkipc
  entry.cgi                         75a693c87c317a49c37c4dddb6b9ac7a    1057168 B  -> /oem/usr/www/cgi-bin/entry.cgi   (M4 control plane, optional)
  sdk/lib/librecamera_ext.so.1.0.0  5cebfb9e4d9c001c45b58c75daafe934      89496 B  -> /oem/usr/lib/  (+ .so.1, .so symlinks)
  sdk/python/recamera_ext/          (ctypes bindings)                              -> /userdata/sdk/python/
  sdk/recamera_ext.h                (C header)                                     -> /userdata/sdk/

PYTHON INFERENCE RUNTIME (rknnlite 2.3.2)  -- provisioned by install.sh into /userdata/rknnenv (venv, --system-site-packages)
  wheels/rknn_toolkit_lite2-2.3.2-cp311-cp311-manylinux_2_17_aarch64.manylinux2014_aarch64.whl                          5da9258e0c69779707e8a06411c398da    569160 B
  wheels/psutil-6.1.1-cp36-abi3-manylinux_2_17_aarch64.manylinux2014_aarch64.whl                                        23b188171fafc49916f352205fdc186a    289017 B
  wheels/ruamel_yaml-0.19.1-py3-none-any.whl                                                                            db65f7e375cd4b465fce6bfad13174e0    118102 B
  wheels/ruamel_yaml_clib-0.2.15-cp311-cp311-manylinux2014_aarch64.manylinux_2_17_aarch64.manylinux_2_28_aarch64.whl    a51fbd45b7d498f276d08fbfa05a3076    749289 B
  (numpy comes from the system site-packages, not a wheel.)
  install.sh also symlinks /usr/lib/librknnrt.so -> /oem/usr/lib/librknnrt.so (stock rknnlite hardcodes that path).
  vision apps run via: PYTHONPATH=/userdata/local:/userdata/sdk/python LD_LIBRARY_PATH=/oem/usr/lib /userdata/rknnenv/bin/python3 <app>.py

FACTORY / ROLLBACK
  rollback target test              CONTENT, not md5: a factory rkipc carries none of
                                    /run/recamera | rc_ext_
                                    (factory entry.cgi carries no ExtApiHandler).
                                    Holds for any baseline -- no md5 list to maintain.
                                    NOT osd_rgn_cover_ -- that is a vendor OSD symbol
                                    present in factory rkipc too (measured: 2 hits in
                                    d5e7ca93 factory, 8 in 9826e9ec ext).
  validated baselines               ce3dfa64a554667028b47f4d9ce84981  (2026-08 factory,
                                    2178232 B, seen on 192.168.42.1). Advisory only:
                                    an unlisted baseline warns; --strict makes it abort.
  no local backup?                  recover with a full OTA / update.img flash, which
                                    rewrites /oem back to factory.
  factory entry.cgi                 saved to /userdata/entry.cgi.factory.bak

CONTENTS OF THIS rkipc
  M1-M4 + items 2/3 ; frame proxy + result injection + probe ; pool depth default N=6 (the G5/G6 PASS build).

EXTENSION ENDPOINTS (appear under /run/recamera/ after install+reboot)
  frame.sock       frame proxy (zero-copy frame handoff to solutions)
  result-in.sock   detection/classification/segmentation/tracking/keypoints injection back into rkipc overlay/stream
  probe.sock       status/probe
  apps.d/          per-app control dir

COMPATIBILITY
  Firmware V1.0.4 and V1.0.10 (cross-build compatible). /oem is ext4 rw -> persistent across normal reboots.

KNOWN GAPS
  - none outstanding for the main path. This rkipc (de5b3aa4) includes the keypoints-over-WebSocket
    decode fix and classification box OSD rendering; frame/result/probe/keypoints paths all active.

BOUNDARY / RISK
  - This is a SIDELOAD overwriting /oem. A full firmware OTA / update.img flash WILL overwrite /oem and revert to factory
    rkipc -> re-run install.sh after any OTA. Rollback (rollback.sh) restores factory rkipc from /userdata backup.
  - Does NOT touch partitions, shadow, update.img.
