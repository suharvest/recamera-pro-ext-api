# 复用 reCamera Pro 界面作为调试/测试面板 — 方案商视角

> 场景：方案商在 reCamera Pro 上跑**自建推理管线**（Tier B，如人脸识别），想把官方 web 界面当**调试面板 / 测试工具**复用。
> 现实约束：官方前端是**闭源 minified React bundle，注入不进去** → 落地方式 = 在 `/appcenter/` 建一个**自己的调试 SPA**，尽量复用官方**服务端**能力（视频/鉴权/日志/终端/快照/存储/配置/结果流），只补管线相关的调试面板。

## 1. 官方现成、可直接复用的积木

| 能力 | 官方接口 / 端口 | 复用方式 |
|---|---|---|
| 视频实时预览 | go2rtc `:1984`(nginx `/go2rtc/`) · WebRTC `:8555` · RTSP `554`/`5554`(main `/live/0`、sub `/live/1`) | 嵌 WebRTC/RTSP 播放器 |
| 登录 / 鉴权 | `localStorage.sensecraft_token` + nginx `auth_request /_jwt_verify` | 复用官方登录，API 带 token |
| 设备信息 | entry.cgi `device-info` | 直接调 |
| 快照抓帧 | entry.cgi `capture`(`rk_video_take_photo_jpeg`) | 抓帧存测试用例 |
| 录制 | entry.cgi `record` / `get-record-status` | 录测试片段 |
| 文件/存储 | entry.cgi `file/files/download/upload` + `/userdata`(剩 10.7G) | 存测试素材/日志/注册库 |
| 内嵌终端 | ttyd `:7681`(nginx `/ws/system/terminal`) | 面板里嵌终端 |
| 系统日志 | WS `:8765`(nginx `/ws/system/logs`) | 实时系统日志 |
| 推理结果流 | WS `:8123`(nginx `/ws/inference/results`) | 订阅官方/你的结果 |
| 画面/OSD/打码/推流配置 | entry.cgi `osd_manager_set_cfg` 等 | 复用画面调试 |
| 网络/NTP/固件 | `network-*` / `ntp` / `firmware-*` | 环境调试 |
| 音频录/放 | `arecord`/`aplay`（`test_record.wav` 证明可用） | 音频类 app 调试 |

## 2. 作为"调试/测试面板"还需补充什么（官方没有，需自建）

### A. 你自己管线的可视化
1. **自有结果叠加**（client-side canvas 叠在视频上）——官方叠加只画 rkipc 模型；你的框/关键点/人名/分割掩码要自己叠。
2. **多阶段中间结果**——FR：检测框 → 对齐脸片 → embedding 相似度 → 匹配身份，逐级看，定位哪级挂了。
3. **原始模型输出查看**——置信度分布、NMS 前后、anchor 命中（深度 debug）。

### B. 指标 / 遥测
4. **管线自己的 FPS / 逐级延迟 / 端到端时延**（官方那个 FPS 是 rkipc 的，不是你的）。
5. **NPU 利用率**(`/proc/rknpu/load`)、内存、温度、**丢帧率**、帧队列深度。
6. 与官方推理并存时的**资源占用对照**。

### C. 事件 / 数据
7. **结果事件日志**（表格，时间戳、可筛选、可导出）。
8. **领域数据管理**——FR：人脸注册库 CRUD（录入/列表/删除/重命名）。
9. **触发/告警联调**——条件命中 + 通知（接 recamera_notify）。

### D. 参数在线调试（最核心的"调试工具"属性）
10. **阈值/参数热调**——conf/NMS/匹配阈值/**忽略区**，改完即时生效看效果。
11. **忽略区/ROI 可视化编辑器**——在预览上画多边形，回传给你的后处理（你已定：忽略区走后处理）。
12. **模型热切换 / A-B 对比**——上传/切换你自己的模型，版本对照。

### E. 测试 / 回归（让它成为"测试工具"而不止"面板"）
13. **测试输入注入**——用静态图/离线视频喂管线（不走实时相机），**可复现测试** ← 官方无，核心缺口。
14. **录制 → 回放**——抓一段真实场景存下，反复回放跑管线做回归。
15. **期望值对比 / 打标**——输出 vs ground-truth，回归差异高亮。
16. **一键导出调试包**——截图+日志+配置+样本，复现/上报。

### F. 生命周期 / 环境
17. **你的 app 启停/重启/模式切换**（接 appmgr）。
18. **你的 app 日志**（stdout/stderr，独立于官方系统日志）。
19. **音频调试**——VU 表/波形/ASR/SED 结果/回放（音频类 app）。

## 3. 每类怎么接

| 补充项 | 复用官方 | 自建（/appcenter SPA） | 依赖官方开口 |
|---|---|---|---|
| 自有结果叠加 | 视频流 | canvas 叠加 + WS 订阅自己结果 | （想画到官方那路叠加才需 L3） |
| 多阶段/张量可视化 | — | 全自建（你的进程吐调试数据） | — |
| 管线 FPS/延迟/丢帧 | — | 你的进程埋点 → WS/HTTP | — |
| NPU/内存/温度 | `/proc/rknpu/load`、sysfs | 读取+展示 | — |
| 事件日志/注册库 | 文件/存储 API + /userdata | 表格 UI + 你的 CRUD | — |
| 参数热调/忽略区编辑 | 视频流(画多边形底图) | 编辑器 → 你的进程热加载 | — |
| 模型热切换 | 文件上传 API | 你的 app reload 逻辑 | — |
| **测试输入注入** | — | file→frame 喂你的管线 | **喂 NPU 需 L1 帧口的反向"file source"，或你进程自己 decode 文件** |
| 录制/回放 | 录制/快照 API + 存储 | 回放调度 | — |
| 启停/日志 | 系统日志 WS、终端 | appmgr 控制 + 你的日志 WS | — |

> 绝大多数**自建在 /appcenter SPA**；视频/鉴权/终端/日志/快照/存储/结果流**复用官方**；只有极少数（把结果画进官方那路叠加、测试图喂官方 NPU 通道）**依赖官方开口**——而你已选"忽略区走后处理、结果走自己客户端叠加"，所以**基本不卡官方**。

## 4. 复用官方界面的三种姿势（务实排序）
- **(a) 独立调试 SPA `/appcenter/`**：嵌官方视频 + 复用官方 API + 补自己面板。**唯一完全可控，推荐。**
- **(b) iframe / 深链官方页**：把官方「实时预览」「终端」页拼进你的面板，轻量拼装。
- **(c) 争取并入官方 dashboard**：加你的 tab，最统一但需 Seeed 配合、不可控。

## 5. MVP 优先级
- **P0（先有能调）**：自有结果叠加、事件日志、管线 FPS/延迟、参数热调、启停控制、app 日志。
- **P1（能测能调优）**：忽略区可视化编辑、测试输入注入、录制回放、模型热切换、NPU/内存遥测。
- **P2（体验/深度）**：多阶段可视化、注册库管理、期望值对比、导出调试包、音频调试。

## 6. 少数依赖官方开口的项（可转 Seeed SDK 需求，见 `RECAMERA_PRO_INFERENCE_SDK_DESIGN.md`）
- **测试图/离线视频喂官方 NPU 通道** → 关联 L1 帧代理加一个"外部喂帧/file-source"反向口（你也可在自己进程 decode 文件绕开）。
- **结果显示在官方服务端叠加**（而非客户端 canvas）→ L3 结果注入。
- **忽略区引擎级生效** → L4.1（你已选后处理，不依赖）。

## 7. 一句话
官方把**视频、鉴权、终端、日志、快照、录制、存储、结果流**这些"通用外壳"都给齐了，方案商复用它们省掉一大半；真正要补的是**"你自己管线的可视化 + 参数热调 + 测试输入/回放"**这类**管线专属的调试/测试能力**——这些天然是你自建的,且大多不依赖官方额外开口。
